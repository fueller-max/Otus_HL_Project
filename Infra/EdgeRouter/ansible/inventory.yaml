all:
  children:
    routers:
      hosts:
        router-edge-1:
          ansible_host: 192.168.70.100

  vars:
    ansible_user: deploy
    ansible_connection: ssh
    ansible_python_interpreter: /usr/bin/python3.12
   
