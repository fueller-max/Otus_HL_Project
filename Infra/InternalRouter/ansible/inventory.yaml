all:
  children:
    routers:
      hosts:
        router-internal-1:
          ansible_host: 192.168.70.101

  vars:
    ansible_user: deploy
    ansible_connection: ssh
    ansible_python_interpreter: /usr/bin/python3.12
   
